name = "brickfrog/sql2m"

version = "0.1.0"

readme = "README.md"

repository = ""

license = "Apache-2.0"

keywords = [ ]

preferred_target = "js"

description = "SQL (DuckDB parse tree) to Power Query M transpiler"
